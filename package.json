{
  "name": "backend-muscle-memory-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Backend Muscle Memory - Practice building backends until it's automatic",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:backend": "cd apps/backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd apps/frontend && npm run build",
    "build:backend": "cd apps/backend && npm run build",
    "install:all": "npm install && cd apps/frontend && npm install && cd ../backend && npm install",
    "clean": "rm -rf apps/frontend/.next apps/backend/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
